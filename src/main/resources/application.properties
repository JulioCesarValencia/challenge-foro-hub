spring.application.name=forohub
# Configuración de la base de datos

spring.datasource.url=jdbc:mysql://localhost:3306/forohub_db
spring.datasource.username=${MYSQL_USER}
spring.datasource.password=${MYSQL_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# esto es para dev, recordar borrar despues al finalizar el proyeto

##spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true

spring.jpa.properties.hibernate.format_sql=true

#spring.flyway.repair-on-error=true



# Propiedades para JWT
app.jwt.secret=F1rm@foro
app.jwt.issuer=forohub_db
app.jwt.expiration-in-minutes=1440